# generated by tools/build-android.sh — never commit it
build/

node_modules/
.DS_Store
*.log

# signing material. Losing these means you can never update your own app,
# so back them up somewhere that is NOT this repo.
*.jks
*.keystore
keystore.properties
play-service-account*.json

# local scratch
/tmp/

# generated: `npm run notes -- --write` and `node tools/build-pages.mjs`
distribution/
site/
